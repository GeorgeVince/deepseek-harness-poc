services:
  chatbot:
    build: .
    init: true
    user: "${UID:-1000}:${GID:-1000}"
    ports:
      - "127.0.0.1:${PORT:-8000}:8000"
    environment:
      HOME: /tmp
      PI_AUTH_FILE: /pi-agent/auth.json
    volumes:
      - "${HOME}/.pi/agent:/pi-agent:ro"
      - ./.dsh:/app/.dsh
